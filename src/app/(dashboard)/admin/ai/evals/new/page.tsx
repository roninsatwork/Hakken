import { NewEvalScreen } from "@/src/app/(dashboard)/admin/_features/evals/NewEvalScreen";

/** Writing a new check for the global AI. */
export default function NewGlobalEvalPage() {
  return <NewEvalScreen />;
}
